# Direct connection to the database
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.pyrwgoagspikhvsoyoye
spring.datasource.password=vyGdejHUJYTWzinS
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration (only used if enableJPA=true)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Feature Flag: Switch between JPA and JDBC Template
# Set to true for JPA, false for JDBC Template
app.persistence.enableJPA=false

debug=true


#REdis ttl variable implementation for caching of the get data for 30 minutes and when the data is updated the cache will be deleted and the ttl variable deletred until the next request